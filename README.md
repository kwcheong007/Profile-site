# Profile-site

Personal portfolio site — plain HTML/CSS with a touch of vanilla JS. No frameworks, no build step.

## Structure

```
public/
  index.html   # single page with sections: hero, about, skills, projects, repos, contact
  style.css
  main.js
```

## Deploy

Push to `main` — GitHub Actions builds and publishes to GitHub Pages automatically.
